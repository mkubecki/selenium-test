pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/mkubecki/selenium-test.git'
            }
        }
        stage('Install Dependencies') {
            steps {
                pip install -r requirements.txt
            }
        }
        stage('Run Tests') {
            steps {
                /Library/Frameworks/Python.framework/Versions/3.12/bin/pytest --html=report.html --self-contained-html
            }
        }
        stage('Publish Report') {
            steps {
                publishHTML(target: [
                    allowMissing: false,
                    alwaysLinkToLastBuild: false,
                    keepAll: true,
                    reportDir: '.',
                    reportFiles: 'report.html',
                    reportName: 'Test Report'
                ])
            }
        }
    }
}